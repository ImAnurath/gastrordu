# Dependencies / build
node_modules/
.next/
out/
build/

# Env
.env
.env*.local

# Raw image assets (large; kept local for now — see Docs/image-catalog.md)
# Anchored to repo root so it does NOT match public/images (git is
# case-insensitive on Windows/macOS and would otherwise ignore it).
/Images/

# OS / editor
.DS_Store
*.log
